<Window x:Class="txat_aurreratua_client.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:txat_aurreratua_client"
        mc:Ignorable="d"
        Title="Txat Aurreratua" Height="500" Width="550">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,10">
            <Button x:Name="btn_konektatu" Content="Zerbitzarira konektatu"
                    Width="180" Margin="0,0,10,0"
                    IsEnabled="False" Click="btn_konektatu_Click"/>
            <Button x:Name="btn_itxi" Content="Konexioa itxi / deskonektatu"
                    Width="180"
                    IsEnabled="False" Click="btn_itxi_Click"/>
        </StackPanel>

        <Grid Grid.Row="1" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <StackPanel Margin="0,0,10,0">
                <Label Content="Zerbitzariaren IP:" />
                <TextBox x:Name="txt_box_IP" Text="127.0.0.1"/>
            </StackPanel>

            <StackPanel Grid.Column="1">
                <Label Content="Izena:" />
                <TextBox x:Name="txt_box_izena" TextChanged="txt_box_izena_TextChanged"/>
            </StackPanel>
        </Grid>

        <GroupBox Grid.Row="2" Header="Txat">
            <Grid Margin="5">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBox x:Name="txt_chat"
                         Grid.Row="0"
                         TextWrapping="Wrap"
                         VerticalScrollBarVisibility="Auto"
                         IsReadOnly="True"/>

                <StackPanel Grid.Row="1"
                            Orientation="Horizontal"
                            Margin="0,5,0,0">
                    <TextBox x:Name="txt_mensaje"
                             Width="400"
                             TextWrapping="Wrap"/>
                    <Button x:Name="btn_bidali"
                            Content="Bidali"
                            Width="103"
                            Margin="5,0,0,0" Click="btn_bidali_Click"/>
                </StackPanel>
            </Grid>
        </GroupBox>

    </Grid>
</Window>
